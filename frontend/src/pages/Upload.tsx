import { Box, Typography } from '@mui/material';

export default function Upload() {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h4" gutterBottom>
        Image Upload
      </Typography>
      <Typography variant="body1" color="text.secondary">
        Image upload interface - coming in Task 5
      </Typography>
    </Box>
  );
}
